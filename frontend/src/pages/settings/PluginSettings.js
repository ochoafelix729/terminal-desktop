import React from "react";

const PluginSettings = () => {
  return (
    <div className="settings-section">
      <h2>Plugin Settings</h2>
      <p>Enable or disable plugins and configure plugin options.</p>
    </div>
  );
};

export default PluginSettings;
