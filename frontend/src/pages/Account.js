import TerminalUI from "../layouts/TerminalUI";

const Account = () => {
    return (
        <div className="terminal-pane">
            <TerminalUI />
        </div>
    );
};

export default Account;
